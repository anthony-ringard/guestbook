{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}

    {% for conference in conferences%}
        <h4>{{ conference  }}</h4>
        <p>
            <a href="{{ path('conference', {id: conference.id }) }}"> Views </a>
        </p>

    {% endfor %}



{% endblock %}
